# This Vagrantfile is autogenerated by uplink from "{{config_name}}".

require "vagrant"

VAGRANT_API_VERSION = "2"
ENV["LC_ALL"] = "en_US.UTF-8"

Vagrant.configure(VAGRANT_API_VERSION) do |config|
  {% for vm in vms %}
  config.vm.define "{{ vm.name }}" do |config|
    config.vm.hostname = "{{ vm.hostname }}"
    config.vm.box = "{{ vm.box }}"
    config.vm.network "private_network", ip: "{{ vm.ip }}"
    config.vm.provision "shell", inline: <<-end
      {% for command in vm.shell_provision -%}
      {{ command }}
      {% endfor %}
    end
  end
  {% endfor %}
  config.ssh.shell = "sh -c 'BASH_ENV=/etc/profile exec bash'"
end

